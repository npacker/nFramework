Request http://localhost/recipe/view/1 from a browser with the app installed.

This will demonstrate the process of fetching a recipe from the database.

Creation and update functionality is not implmented and neither has the view layer.

Apache mod_rewrite and overrides must be enabled.

The database 'recipesdb' must be manually created.